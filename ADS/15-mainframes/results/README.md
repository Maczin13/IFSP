# Resultados observados

`batch-report.json` contém registros aceitos, rejeitados e reconciliação. `accepted.dat` mantém largura fixa; `rejected.dat` registra linha, motivo e conteúdo bruto. `pytest-run.log` registra a suíte do copybook e das regras batch.
