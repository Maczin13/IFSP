# Resultados observados

`audit.json` é gerado a partir de `controls.csv` e contém resumo, taxa de aprovação, maturidade média e avaliação individual dos controles. `pytest-run.log` registra a suíte de regras. Os dados são sintéticos e não constituem auditoria de compliance.
