# Resultados observados

`pytest-run.log` registra testes de escrita local, Outbox, retry, reconexão, versionamento e preservação offline. `state.json` é gerado pela CLI. Os artefatos são uma simulação acadêmica, não uma aplicação Android publicada.
