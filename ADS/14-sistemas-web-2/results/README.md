# Resultados observados

`pytest-run.log` registra requisições HTTP via TestClient, validação e status. `openapi.json` é gerado pela aplicação para documentar o contrato. Os testes não afirmam autenticação, rate limiting ou persistência de produção.
