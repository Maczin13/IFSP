# Resultados observados

`metrics.json` contém CPM e EVM gerados por `data/project.json`. `pytest-run.log` registra testes normais, ciclos, predecessores ausentes, atrasos e desvios de custo. Os valores são sintéticos e servem para reprodução do laboratório.
