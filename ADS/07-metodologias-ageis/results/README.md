# Resultados observados

`pytest-run.log` registra a suíte de regras do tracker. `metrics.json` é gerado a partir do `backlog.csv` e contém distribuição de estados, pontos concluídos, velocity e médias de Lead/Cycle Time. Os dados são sintéticos e não representam um time real.
